services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ENV: production
      APP_HOST: 0.0.0.0
      APP_PORT: 8080
      APP_RELOAD: "false"
      DATABASE_FILE: /app/data/app.db
    volumes:
      - ./data:/app/data
